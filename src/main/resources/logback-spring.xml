<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Converter bawaan Spring Boot untuk stack trace -->
    <conversionRule conversionWord="wEx"
        converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter" />

    <!-- Converter untuk replace -->
    <conversionRule conversionWord="replace"
        converterClass="ch.qos.logback.core.pattern.ReplacingCompositeConverter" />

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n
                %replace(%wEx){
                '(?m)^\s*at (org\.apache\.(catalina|tomcat|coyote)|org\.apache\.tomcat\.util)\..*$',
                '\t... [container frames suppressed]'
                }%n
            </pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/hospital-app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/hospital-app.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
                %replace(%wEx){
                '(?m)^\s*at (org\.apache\.(catalina|tomcat|coyote)|org\.apache\.tomcat\.util)\..*$',
                '\t... [container frames suppressed]'
                }%n
            </pattern>
        </encoder>
    </appender>

    <root level="WARN">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- Application specific logging -->
    <logger name="com.example.hospital" level="INFO" />
    <logger name="org.springframework.web" level="WARN" />

    <!-- Suppress Tomcat container noise -->
    <logger name="org.apache.catalina" level="WARN" />
    <logger name="org.apache.coyote" level="WARN" />
    <logger name="org.apache.tomcat" level="WARN" />
    <logger name="org.apache.tomcat.util" level="WARN" />
</configuration>
